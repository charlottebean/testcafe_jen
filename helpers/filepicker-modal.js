import { Selector, t } from 'testcafe';
import { ExperienceCommonPage } from '../pages';
const experienceCommonPage = new ExperienceCommonPage();

export default class Filepicker {
  async uploadPhoto(url) {
  }
}
